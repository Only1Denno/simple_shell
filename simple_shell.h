#ifndef _SIMPLE_SHELL_H_
#define _SIMPLE_SHELL_H_

/*
 * header files for simple shell
 */

#include <stdio.h> /* input output */
#include <unistd.h> /* universal standard */
#include <stdlib.h> /* standard library */
#include <sys/types.h> /* PID PPID */
#include <fcntl.h> /* defines the value for command argument */
#include <sys/wait.h>  /* wait for proess to change state */
#include <sys/stat.h> /* get status of file */
#include <signal.h> /* handles different signals */
#include <errno.h> /*report and retrieve error conditions */

#endif
